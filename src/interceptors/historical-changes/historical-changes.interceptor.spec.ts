import { HistoricalChangesInterceptor } from './historical-changes.interceptor';

describe('HistoricalChangesInterceptor', () => {
  it('should be defined', () => {
    expect(new HistoricalChangesInterceptor()).toBeDefined();
  });
});
